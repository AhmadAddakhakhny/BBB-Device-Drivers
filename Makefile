# Root of the project
SRC_DIR := $(CURDIR)
ARM_COMPILER_PATH := $(SRC_DIR)/sdk/sysroot/bin
# BUILD_DIR := $(SRC_DIR)/builds/$(ARCH)/$(BUILD-TYPE)
# INSTALL_DIR := $(SRC_DIR)/install
# INSTALL_PREFIX := $(INSTALL_DIR)/$(ARCH)/$(BUILD-TYPE)
# CONAN_DIR := $(SRC_DIR)/external
